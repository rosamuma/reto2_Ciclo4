import 'package:flutter/cupertino.dart';

class Productos {
  final id;
  final nombre;
  final image;
  int precio;
  int cantidad;

  Productos(this.id, this.nombre, this.image, this.precio, this.cantidad);
  
}